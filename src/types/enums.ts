export enum HttpMethods {
    GET = 'GET',
    POST = 'POST'
}

export enum HttpCodes {
    OK = 200,
    NOT_FOUND = 404,
    SERVER_ERROR = 500
}

export enum Layouts {
    UPLOAD_FILE = 'upload_file.html'
}